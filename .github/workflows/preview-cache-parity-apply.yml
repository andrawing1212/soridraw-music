name: RETIRED - Preview Cache Parity Apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired unbounded cache migration
        run: |
          echo 'BLOCKED: this retired workflow could rebuild a user whole song collection.'
          echo 'SORIDRAW cold start must use bounded page + cache/delta only.'
          exit 1

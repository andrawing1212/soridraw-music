# Fixed SORIDRAW release trigger. Keep disabled unless the user explicitly approves a release.
# Legacy trigger retained only as an inert audit marker. The Release Controller is
# manual-dispatch only and accepts preflight_only, test, or production inputs.
enabled=false

export const EMAIL_VERIFICATION_ACTION_SETTINGS = {
  // The Firebase email template action URL is configured separately to
  // https://soridraw.web.app/auth/action. This value is only the safe return URL.
  url: 'https://soridraw.web.app/',
  handleCodeInApp: false,
} as const;

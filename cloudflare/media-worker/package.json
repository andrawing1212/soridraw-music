{
  "name": "soridraw-media-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}

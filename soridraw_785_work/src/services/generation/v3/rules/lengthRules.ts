import type { V3SourceParams } from '../types';

/** Converts the selected lyric length into a minimal non-creative constraint. */
export function buildV3LengthRule(_params: V3SourceParams): string {
  return '';
}

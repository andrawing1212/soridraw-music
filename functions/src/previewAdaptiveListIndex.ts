import { HttpsError, onCall } from "firebase-functions/v2/https";
import * as admin from "firebase-admin";

// Obsolete source placeholder. The adaptive callable export was removed by the common R2 catalog engine migration.
// Kept temporarily only if direct deletion cannot be applied through the current GitHub connector.
export {};

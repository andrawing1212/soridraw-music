SORIDRAW production deployment trigger.

This file is updated only after the user explicitly requests an official Firebase production deployment.
Updating this file triggers the GitHub Actions workflow that builds the latest main branch and deploys Firebase Hosting only.

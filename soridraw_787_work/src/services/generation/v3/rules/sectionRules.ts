import type { V3SourceParams } from '../types';

/** Returns only the user-selected section order. No lyric content is created. */
export function buildV3SectionRule(_params: V3SourceParams): string {
  return '';
}

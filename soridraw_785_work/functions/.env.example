# Set to true only after the registered Firebase web app returns valid App Check tokens
# from every deployed domain. Keep false while any environment reports missing or invalid.
ENFORCE_APP_CHECK=false
